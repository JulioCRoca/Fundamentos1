#include <iostream>
#include <math.h>
using namespace std;
int main()
{
    int x,n,s=0;
    do{
    cout<<"Ingrese un numero: ";
    cin>>x;
    cout<<"Ingrese el exponente: ";
    cin>>n;
    }while((x<=0) || (n<0));
    for(int i=1;i<=n;i++)
    {
        s=s+pow(x,i);
    }
    if(
    cout<<"La suma es de: "<<s;
    return 0;
}
